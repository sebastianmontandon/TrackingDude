{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && npm run build",
  "functions": {
    "pages/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1",
    "SKIP_ENV_VALIDATION": "true"
  }
} 